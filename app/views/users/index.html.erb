<h1>Welcome, <%= current_user.username %></h1>

<%= button_to "New Story", new_story_path, method: :get %>

<hr>

<% @stories.each do |story| %>
  <h4><%= link_to story.display_title, story_path(story) %></h4>
<% end %>

